<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nongcetong.nongcetongbackend.mapper.UserMapper">

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.nongcetong.nongcetongbackend.entity.User">
        INSERT INTO user (username, password, role, create_time)
        VALUES (#{username}, #{password}, #{role}, NOW())
    </insert>

    <!-- 根据用户名查询 -->
    <select id="selectByUsername" resultType="com.nongcetong.nongcetongbackend.entity.User">
        SELECT id, username, password, role, create_time
        FROM user
        WHERE username = #{username}
    </select>
</mapper>